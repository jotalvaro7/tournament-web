/**
 * Environment Configuration
 *
 * Production environment settings
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.tournament-manager.com/api' // TODO: Replace with actual production URL
};
